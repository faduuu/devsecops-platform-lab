---
extends: default

rules:
  document-start: disable
  empty-lines:
    max: 2
    max-start: 0
    max-end: 1
  line-length:
    max: 120
    level: warning
  new-lines: disable
  truthy:
    allowed-values:
      - "true"
      - "false"
      - "on"
    check-keys: false
